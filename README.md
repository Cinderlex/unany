# UNany

There are a lot of general-purpose types and operations that can be useful for development. This repo in its current state is a playground for type-safe implementation of such things. It is now in a state of flux, so use at your own risk!

Installation

`npm i @cinderlex/unany`

`yarn add @cinderlex/unany`
